const frameWidth = 510;

export const poemLineCount = 18;
export const frame = {
    width: frameWidth
};
export const sectionSelector = {
    verticalPadding: 18,
    snapAfter: 2000,
    width: frameWidth
};
